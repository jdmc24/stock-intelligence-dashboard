# Frontend

Next.js (App Router) app for the Stock Intelligence Dashboard. See the
[repo root README](../README.md) for project overview, architecture,
and local development instructions.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
